import type { JSX } from "react";

export default function Header(): JSX.Element {
  return (
    <header>
      <h1>Valorant: Agent Lobby</h1>
      <p>
        Guess the word within 8 attempts to get your instalock character pick.
      </p>
    </header>
  );
}
